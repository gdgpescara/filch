import 'package:flutter/material.dart';

class StaffLayout extends StatelessWidget {
  const StaffLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Staff Dashboard Layout'),
    );
  }
}
