import 'package:flutter/material.dart';

class UserPointsWidget extends StatelessWidget {
const UserPointsWidget({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
return Container();
}
}
