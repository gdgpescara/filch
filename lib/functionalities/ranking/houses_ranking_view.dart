import 'package:flutter/material.dart';

class HousesRankingView extends StatelessWidget {
  const HousesRankingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
